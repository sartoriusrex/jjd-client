import React from 'react';
import { shallow } from 'enzyme';
import ShareItem from './ShareItem';

// it('ShareItem renders without crashing', () => {
//   shallow( <ShareItem /> );
// });

it ('sharetest', () => {
	expect(true).toBeTruthy();
});